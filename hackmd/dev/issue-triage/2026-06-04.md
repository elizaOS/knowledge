# Issue Triage — 2026-06-04 (elizaOS)

## 1) Orchestrator: sub-agent completion narration drops/garbles deliverable (narration-envelope) — **#8162**
- **Current Status:** OPEN (3 comments). Repro’d across multi-backend sweep; underlying work correct, presentation broken.
- **Impact Assessment:**
  - **User Impact:** **High** (anyone using orchestrator/coding workbench; common “print output” tasks)
  - **Functional Impact:** **Partial** (core work completes, but user cannot reliably see results)
  - **Brand Impact:** **High** (appears broken/unreliable; leaks process chatter; truncation)
- **Technical Classification:**
  - **Category:** Bug / UX
  - **Component:** Plugin Agent Orchestrator (sub-agent-router, completion evaluator), Planner loop response handling
  - **Complexity:** **Moderate effort** (needs careful carrier + filtering to avoid breaking respawn logic)
- **Resource Requirements:**
  - **Required Expertise:** Orchestrator event routing, evaluator pipeline, message rendering, test design
  - **Dependencies:** Coordinate with recent `develop` rewrites in `sub-agent-router.ts` and `evaluators/sub-agent-completion.ts`
  - **Estimated Effort:** **3/5**
- **Recommended Priority:** **P1**
- **Specific Actionable Next Steps:**
  1. Implement **verbatim deliverable carrier** (`metadata.subAgentDeliverable`, ≤2KB) on `task_complete && !changeSet && verifiedUrls.length===0` **before** tool transcript stripping.
  2. Update evaluator `sub-agent-completion` to **prefer verbatim relay** when carrier present (parallel to verified URL handling).
  3. Design + implement **terminal-attempt-only rendering** to suppress stale failed/blocked attempts while preserving internal respawn triggers (split internal signals vs user narration).
  4. Add regression tests:
     - “no change-set printed-value task” must show stdout
     - “retry then success” must not show stale failure narration
     - truncation guard on weak planners
- **Potential Assignees:** **NubsCarson** (reporter, has patch), **odilitime** (core/orchestrator), **stan0473** (core dev support)

---

## 2) CI: Plugin Tests failing — `@elizaos/plugin-rlm` spawns `/nonexistent/python` (untracked) — **NEW-2026-06-04-CI-01**
- **Current Status:** Not tracked as a GitHub issue in provided data; repeatedly referenced as red lane on `develop` in PR context.
- **Impact Assessment:**
  - **User Impact:** **High** (blocks contributors; slows merges; increases drift)
  - **Functional Impact:** **Yes** (CI gate blocks PR throughput and “trust in mainline”)
  - **Brand Impact:** **Medium/High** (project appears unstable to external contributors)
- **Technical Classification:**
  - **Category:** Bug / Developer Experience
  - **Component:** CI + plugin-rlm test harness
  - **Complexity:** **Simple fix** (fix test to use fixture python or skip when absent; or mock spawn)
- **Resource Requirements:**
  - **Required Expertise:** CI, Vitest/test isolation, plugin-rlm internals
  - **Dependencies:** None, but should land quickly to unstick parallel work
  - **Estimated Effort:** **2/5**
- **Recommended Priority:** **P0**
- **Specific Actionable Next Steps:**
  1. Create GitHub issue with exact failing test name + logs.
  2. Patch test to:
     - resolve python path via env (`PYTHON`, `python3`) with fallback skip, **or**
     - mock the python invocation completely.
  3. Add CI guard to prevent hardcoded non-existent paths.
- **Potential Assignees:** **NubsCarson** (already triaging CI lanes), **lalalune** (testing/infra contributions), **standujar** (CI discipline)

---

## 3) CI: Client Tests failing — `FineTuningTuiView.test.tsx` (plugin-training) (untracked) — **NEW-2026-06-04-CI-02**
- **Current Status:** Untracked in provided data; cited as persistent red lane on `develop`.
- **Impact Assessment:**
  - **User Impact:** **Medium/High** (blocks UI-focused PRs; discourages contributions)
  - **Functional Impact:** **Partial** (non-runtime, but CI gate blocks merges)
  - **Brand Impact:** **Medium**
- **Technical Classification:**
  - **Category:** Bug / Test reliability
  - **Component:** UI tests, plugin-training
  - **Complexity:** **Moderate effort** (likely timing/DOM environment/fixture stability)
- **Resource Requirements:**
  - **Required Expertise:** React testing, Vitest/jsdom, plugin-training UI
  - **Dependencies:** Might depend on recent UI shell/overlay changes landing order
  - **Estimated Effort:** **3/5**
- **Recommended Priority:** **P0**
- **Specific Actionable Next Steps:**
  1. Create GitHub issue capturing failing assertions and recent commit range when it started.
  2. Stabilize: remove real timers/network; add deterministic fixtures; avoid relying on layout measurements in jsdom.
  3. Ensure the test suite runs in isolation and in parallel without shared state.
- **Potential Assignees:** **lalalune** (UI/testing), **NubsCarson** (CI triage), **Dexploarer** (cross-platform/test hygiene)

---

## 4) CI: `env:audit:check` fails due to `@discordjs/opus` prebuilt binary 404 (upstream/native flake) (untracked) — **NEW-2026-06-04-CI-03**
- **Current Status:** Mentioned as recurring CI/upstream failure; not an elizaOS runtime bug but blocks gates.
- **Impact Assessment:**
  - **User Impact:** **High** (PRs fail for reasons unrelated to changes)
  - **Functional Impact:** **Yes** (CI gating failure)
  - **Brand Impact:** **Medium**
- **Technical Classification:**
  - **Category:** Build/CI / Dependency management
  - **Component:** Tooling, dependency install pipeline
  - **Complexity:** **Moderate effort** (needs robust fallback strategy)
- **Resource Requirements:**
  - **Required Expertise:** Node native deps, CI caching, optional dependency strategy
  - **Dependencies:** Might require aligning Discord voice features as optional in CI
  - **Estimated Effort:** **3/5**
- **Recommended Priority:** **P0**
- **Specific Actionable Next Steps:**
  1. Create issue with failing platform/arch and dependency version.
  2. Mitigate quickly via one of:
     - mark opus as optional for audit lane, or
     - pin to known-good version, or
     - add fallback to build-from-source with required toolchain, or
     - split env audit lane so native optional deps don’t hard-fail unrelated PRs.
  3. Add CI cache for prebuilt artifacts where legally/technically feasible.
- **Potential Assignees:** **standujar** (CI/infrastructure), **0xSolace** (cloud/build pipelines), **Dexploarer** (release/build systems)

---

## 5) CI: Android Debug Build fails deep-link audit (`shortcuts.xml`) (untracked) — **NEW-2026-06-04-CI-04**
- **Current Status:** Cited as a pre-existing red lane.
- **Impact Assessment:**
  - **User Impact:** **Medium** (mobile contributors blocked; mobile release confidence reduced)
  - **Functional Impact:** **Partial** (not blocking server runtime, but blocks merges if gated)
  - **Brand Impact:** **Medium** (mobile support perceived as unstable)
- **Technical Classification:**
  - **Category:** Bug / Build
  - **Component:** Mobile (Android), CI audit scripts
  - **Complexity:** **Moderate effort**
- **Resource Requirements:**
  - **Required Expertise:** Android app config, deep links, CI build scripts
  - **Dependencies:** None
  - **Estimated Effort:** **3/5**
- **Recommended Priority:** **P1** (P0 if Android lane is required for all merges)
- **Specific Actionable Next Steps:**
  1. Create issue with the audit output and the exact failing rule.
  2. Fix `shortcuts.xml` / manifest to satisfy audit (or adjust audit if false-positive).
  3. Add a targeted unit/lint check locally so developers catch it before CI.
- **Potential Assignees:** **Dexploarer** (release/build), **NubsCarson** (CI coordination), **Oxygen56** (if active on platform builds)

---

## 6) Cloud auth: staging magic-link sign-in fragility (CSP/CORS/app-auth context) — add regression coverage (untracked) — **NEW-2026-06-04-CLOUD-01**
- **Current Status:** Reported as **fixed** on 2026-06-03 via CSP/CORS/app-auth handling changes; risk of regression remains (multi-step cross-origin flow).
- **Impact Assessment:**
  - **User Impact:** **High** (blocks login)
  - **Functional Impact:** **Yes** (cannot access Cloud)
  - **Brand Impact:** **High** (auth failures are credibility killers)
- **Technical Classification:**
  - **Category:** Bug prevention / Reliability
  - **Component:** Cloud Frontend + Cloud API auth endpoints + headers/CSP
  - **Complexity:** **Moderate effort**
- **Resource Requirements:**
  - **Required Expertise:** Web auth flows, Cloudflare Workers, CSP/CORS, e2e testing
  - **Dependencies:** Needs stable staging environment URLs and test email-token harness
  - **Estimated Effort:** **3/5**
- **Recommended Priority:** **P1**
- **Specific Actionable Next Steps:**
  1. Create issue to add an **end-to-end staging auth regression test** (magic-link callback handling, app-auth context creation, CORS/CSP validation).
  2. Add monitoring/alerting for spikes in auth callback failures (4xx/5xx and “Failed to fetch”).
  3. Document required allowlists (`connect-src`, CORS origins) and where they live.
- **Potential Assignees:** **standujar** (cloud), **0xSolace** (cloud services), **NubsCarson** (test rigor), **2-A-M** (if active on cloud frontend)

---

## 7) Feature: Comprehensive Kamino (Solana) plugin to close DeFi integration gap (untracked proposal) — **NEW-2026-06-04-FEAT-01**
- **Current Status:** Proposed on Discord (2026-06-02). Early actions written; guidance given to develop in-repo plugin workflow.
- **Impact Assessment:**
  - **User Impact:** **Medium** now, **High** strategic (enables real DeFi actions vs info retrieval)
  - **Functional Impact:** **No** (new capability), but unlocks core “agent does things” use cases in Web3
  - **Brand Impact:** **Medium/High** (strong integrations improve ecosystem credibility)
- **Technical Classification:**
  - **Category:** Feature Request
  - **Component:** Plugin System / Model Integration with on-chain ops (Solana)
  - **Complexity:** **Complex solution** (safe abstractions, risk controls, DX, transaction signing)
- **Resource Requirements:**
  - **Required Expertise:** Solana/Kamino protocol, transaction building, risk management UX, plugin action design
  - **Dependencies:** Decision on abstraction strategy (protocol-specific vs aggregator layer); wallet/signing standards; permissions/confirmations
  - **Estimated Effort:** **5/5**
- **Recommended Priority:** **P2** (plan near-term; start scaffolding/spec now)
- **Specific Actionable Next Steps:**
  1. Open a tracking issue with an MVP scope: **lend/borrow + vault deposit/withdraw + position health + safe defaults**.
  2. Define “risk-sensitive operation” interface: required confirmations, slippage/health-factor guards, simulation hooks.
  3. Establish test strategy: devnet integration tests + mocked RPC for unit tests.
  4. Align with plugin repo structure in `elizaos/eliza` + rebase cadence.
- **Potential Assignees:** **n4vnt** (driver, per Discord), **anton_0413** (design/abstractions), **stan0473** (plugin process/core guidance)

---

## 8) Documentation: clarify ecosystem/project relationships (ElizaOS vs Hyperia/Hyperforge; ElizaCloud/Milady/Waifu) — **NEW-2026-06-04-DOC-01**
- **Current Status:** Repeated community confusion in Discord; clarifications provided ad hoc by mods.
- **Impact Assessment:**
  - **User Impact:** **Medium** (newcomer confusion; misrouted support)
  - **Functional Impact:** **No**
  - **Brand Impact:** **Medium** (perceived lack of clarity/organization)
- **Technical Classification:**
  - **Category:** Documentation / UX
  - **Component:** Docs + community onboarding
  - **Complexity:** **Simple fix**
- **Resource Requirements:**
  - **Required Expertise:** Product knowledge, docs writing
  - **Dependencies:** None (confirm messaging with maintainers/mods)
  - **Estimated Effort:** **1/5**
- **Recommended Priority:** **P2**
- **Specific Actionable Next Steps:**
  1. Add a canonical doc page: “Project Map” (ElizaOS, ElizaCloud, Milady, Waifu; and explicit “not affiliated” notes where applicable).
  2. Link it in README + Discord pinned post + “Getting Started”.
  3. Add a lightweight FAQ snippet for recurring questions (“What happened to Hyperforge?”).
- **Potential Assignees:** **odilitime** (community ops/core dev), **stan0473** (moderation), **lalalune** (docs polish)

---

# Top Highest-Priority (Fix Now / This Sprint)

1. **P0 — NEW-2026-06-04-CI-01:** CI Plugin Tests failing (`plugin-rlm` `/nonexistent/python`)
2. **P0 — NEW-2026-06-04-CI-02:** CI Client Tests failing (`FineTuningTuiView.test.tsx`)
3. **P0 — NEW-2026-06-04-CI-03:** CI `env:audit:check` failing due to `@discordjs/opus` prebuilt 404
4. **P1 — #8162:** Orchestrator narration-envelope drops/garbles deliverable
5. **P1 — NEW-2026-06-04-CI-04:** Android Debug Build deep-link audit failure (`shortcuts.xml`)
6. **P1 — NEW-2026-06-04-CLOUD-01:** Add regression coverage/monitoring for staging magic-link auth flow

(Then: **P2 Kamino plugin**, **P2 ecosystem relationship docs**.)

---

# Patterns / Themes Observed

- **Reliability gap between “work succeeded” and “user saw the result”:** Orchestrator completion rendering, tool transcript stripping, and weak-planner truncation are repeatedly implicated.
- **CI brittleness / “develop is red” signaling:** Multiple unrelated lanes failing at once (plugin tests, client tests, env audit, Android) creates merge paralysis and increases long-lived PR drift.
- **Auth and environment coupling:** Cloud auth fixes required coordinated CSP/CORS/frontend state handling, suggesting the system is sensitive to small cross-surface config mismatches.

---

# Process Improvement Recommendations

1. **Declare a “green mainline” policy for `develop`:** if `develop` is red for >24h, prioritize CI fixes over feature merges (explicit rotation/ownership).
2. **Convert “untracked red lanes” into first-class issues automatically:** a bot that opens/updates issues when CI lanes fail on `develop` for N consecutive runs, attaching logs and blame ranges.
3. **Add regression tests for high-visibility UX correctness:** especially orchestrator “deliverable integrity” tests (stdout-only tasks, retry flows, truncation guards).
4. **Stabilize dependency/audit lanes:** separate “optional native deps” from “required supply-chain checks,” or allow controlled fallbacks so unrelated PRs don’t fail.
5. **Centralize canonical docs for ecosystem structure:** reduce repeated Discord clarifications and support misrouting.