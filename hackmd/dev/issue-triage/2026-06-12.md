# Issue Triage — 2026-06-12

## 1) Eliza Cloud Apps: scaling + ingress gaps before go-live (audit, evidenced) — #8321
- **Current Status:** OPEN (commentCount: 6). Audit contains explicit P0/P1/P2 gap list; multiple follow-up PRs referenced in repo activity, but this tracking issue remains open.
- **Impact Assessment**
  - **User Impact:** **Critical** (go-live readiness; risk of “apps unreachable” / severe capacity ceilings)
  - **Functional Impact:** **Yes** (blocks Cloud Apps production readiness; may also undermine tenant isolation if env-var contract is wrong)
  - **Brand Impact:** **High** (shipping “Cloud Apps” that don’t route or scale is highly visible)
- **Technical Classification**
  - **Issue Category:** Bug / Performance / Security (mixed; includes infra, networking, billing idempotency, isolation hardening)
  - **Component Affected:** Cloud Infrastructure (Hetzner/Terraform), Apps Data Plane, Ingress/Routing, Postgres pooling, Billing cron
  - **Complexity:** **Architectural change** (multi-node ingress, connection pooling, scheduler/autoscaler integration, lifecycle correctness)
- **Resource Requirements**
  - **Required Expertise:** Terraform/Hetzner, Docker networking, Caddy/ingress, Postgres tuning + pgbouncer, Cloudflare DNS/LB, Worker cron/billing correctness
  - **Dependencies / Blockers:**
    - Hetzner project quota/server limits can block apply (called out in issue; ensure quota resolved before multi-node tests)
    - Ingress must exist before any meaningful reachability/load testing
    - Billing idempotency needs DB constraint alignment (may require migration)
  - **Estimated Effort (1–5):** **5**
- **Recommended Priority:** **P0**
- **Specific Actionable Next Steps**
  1. **Confirm P0 closure status vs production:** verify that (a) apps ingress is installed and actively routing `<id>.apps...` to containers, (b) tenant DB connectivity works across real app images, (c) DB is actually used (no silent fallback).
  2. **Implement Postgres connection scalability:** add **pgbouncer** (or equivalent) and set sane `max_connections`; align plugin-sql pool defaults for multi-tenant apps.
  3. **Multi-node correctness:** fix container placement strategy (stop pinning to node[0]); ensure wildcard DNS / LB supports node >1.
  4. **Security hardening that is currently “written but unused”:** ensure nftables/firewall rules are **actually applied** (and validated with integration tests).
  5. **Billing cron safety:** add unique constraint for `(container_id, billing_period)` and make cron resumable/idempotent; remove inline emails from the critical loop if it risks worker wall-time.
- **Potential Assignees**
  - **Primary:** NubsCarson (apps data-plane audit + implementation context), standujar (cloud infra + deploy ownership)
  - **Supporting:** 2-A-M (billing surfaces), lalalune (core/cloud refactors), Dexploarer (infra/test harness support)

---

## 2) Steward login broken in prod: OAuth authorize 400 (code_challenge) + email/send 400 — blocks all Sign in with Eliza Cloud — #8257
- **Current Status:** OPEN (commentCount: 4). Report indicates prod break affecting both OAuth and email flows.
- **Impact Assessment**
  - **User Impact:** **Critical** (blocks *all* “Sign in with Eliza Cloud” flows: dashboard + CLI login + third-party apps)
  - **Functional Impact:** **Yes** (core authentication pathway)
  - **Brand Impact:** **High** (users cannot log in; immediate trust hit)
- **Technical Classification**
  - **Issue Category:** Bug / Security (auth contract mismatch; PKCE requirement enforcement)
  - **Component Affected:** Cloud Auth (Steward integration), cloud-frontend login, cloud-api auth proxy/exchange
  - **Complexity:** **Moderate effort** (contract alignment + prod verification + coordination with Steward service)
- **Resource Requirements**
  - **Required Expertise:** OAuth2/OIDC + PKCE, cloud-frontend, cloud-api edge worker, Steward upstream behavior
  - **Dependencies / Blockers:** May require changes in **Steward service** (outside repo) for email/send 400; needs upstream coordination and prod rollout window
  - **Estimated Effort (1–5):** **4**
- **Recommended Priority:** **P0**
- **Specific Actionable Next Steps**
  1. **Reproduce in prod with scripted checks** (curl) for:
     - `/steward/auth/oauth/.../authorize` response_type=code with PKCE
     - `/steward/auth/email/send` 400 details (log correlation IDs)
  2. **Confirm deployed state vs code state:** ensure the prod frontend actually ships PKCE parameters and exchange path supports verifier forwarding.
  3. **Coordinate with Steward team** on the email/send 400 (validate request schema changes, rate limits, tenant config, or required headers).
  4. Add **monitoring + synthetic** login probes (OAuth + magic link) so future auth regressions page quickly.
- **Potential Assignees**
  - **Primary:** standujar (cloud deploy + auth fixes track record), 0xSolace (Steward session/auth integration), NubsCarson (auth contract analysis)
  - **External/Coordination:** Steward service owners

---

## 3) Android build synchronization blocked (web-less APK builds, Capacitor plugin registration, capacitor.plugins.json sync, asset server path resolution) — **ID: TBD (needs tracking issue)**
- **Current Status:** **Untracked as a GitHub issue in provided data**; called out as “blocked issue” in development logs (active focus).
- **Impact Assessment**
  - **User Impact:** **High** (Android builds are a major distribution channel; blocks mobile releases and contributor validation)
  - **Functional Impact:** **Partial** (core framework runs, but mobile build/release pipeline is blocked)
  - **Brand Impact:** **Medium–High** (mobile reliability is a visible maturity signal)
- **Technical Classification**
  - **Issue Category:** Bug / Build-System / DX
  - **Component Affected:** Mobile (Android), Capacitor integration, asset serving/runtime pathing
  - **Complexity:** **Complex solution** (multi-surface: build tooling + runtime asset resolution)
- **Resource Requirements**
  - **Required Expertise:** Android/Gradle, Capacitor internals, build pipeline scripting, asset bundling/serving, CI mobile builds
  - **Dependencies / Blockers:** Needs a single canonical source of truth for plugin registration + generated files; may depend on how web-less packaging is designed
  - **Estimated Effort (1–5):** **5**
- **Recommended Priority:** **P1** (P0 if a release is imminent or CI is fully blocked)
- **Specific Actionable Next Steps**
  1. **Create a dedicated GitHub issue** with a minimal repro matrix:
     - web-less APK vs standard
     - debug vs release
     - clean build vs incremental
  2. Decide and document the canonical generation flow for:
     - Capacitor plugin registration
     - `capacitor.plugins.json` ownership (generated vs committed)
  3. Add CI coverage: one Android build job that validates plugin registration + asset server base path.
  4. Add a “doctor” style check that fails fast when generated files drift.
- **Potential Assignees**
  - **Primary:** Dexploarer (mobile onboarding/build work), lalalune (systematic refactors/ownership boundaries)
  - **Supporting:** standujar (CI pipeline), NubsCarson (cross-surface orchestration if it touches cloud/mobile auth)

---

## 4) Eliza Cloud Apps: Billing cron idempotency + scale concerns (subset of #8321 item #10) — **Tracked under #8321**
- **Current Status:** Covered in #8321; risk remains until enforced by schema + operational controls.
- **Impact Assessment**
  - **User Impact:** **High** (risk of double-billing or incomplete billing runs at scale)
  - **Functional Impact:** **Partial** (doesn’t always crash, but can cause incorrect charges)
  - **Brand Impact:** **High** (billing correctness is existential)
- **Technical Classification**
  - **Issue Category:** Bug / Reliability
  - **Component Affected:** Cloud API Worker cron, billing repositories/schema
  - **Complexity:** **Moderate effort**
- **Resource Requirements**
  - **Required Expertise:** Worker runtime constraints, DB migrations/constraints, idempotency patterns
  - **Dependencies / Blockers:** Database migration for unique constraint; operational guardrails for cron retries
  - **Estimated Effort (1–5):** **3**
- **Recommended Priority:** **P0** (because it affects money movement; execute in lockstep with Apps go-live)
- **Specific Actionable Next Steps**
  1. Add DB-level **unique constraint** on `(container_id, billing_period)`.
  2. Make cron resumable with checkpoints; ensure retries do not re-bill.
  3. Extract side effects (emails) out of the critical billing loop.
- **Potential Assignees**
  - **Primary:** 2-A-M (billing/API surfaces), standujar (cloud deploy), NubsCarson (apps platform coupling)

---

## 5) Third-party plugin registry review: @usenami/plugin-signer entry — PR #8294 (review queue item)
- **Current Status:** PR open per Discord summary (CI passing; awaiting review by “Shaw’s agent” per process notes).
- **Impact Assessment**
  - **User Impact:** **Medium** (unblocks plugin discovery/installation via registry)
  - **Functional Impact:** **No** (ecosystem improvement, not core stability)
  - **Brand Impact:** **Low–Medium** (responsiveness to contributors)
- **Technical Classification**
  - **Issue Category:** Documentation / Ecosystem
  - **Component Affected:** Plugin Registry
  - **Complexity:** **Simple fix** (review/merge)
- **Resource Requirements**
  - **Required Expertise:** Registry policy/compliance, security review of metadata/claims
  - **Dependencies / Blockers:** None; ensure license/source links valid
  - **Estimated Effort (1–5):** **1**
- **Recommended Priority:** **P2**
- **Specific Actionable Next Steps**
  1. Perform registry entry validation (package name/version, repo URL, license, capability description).
  2. Merge if compliant; add a brief note in release notes/community update to encourage more plugin submissions.
- **Potential Assignees**
  - **Primary:** Registry reviewers (Shaw’s agent process), odilitime (community ops + contributor support), lalalune (ecosystem stewardship)

---

## 6) Local gRPC pipeline for token optimization (stress test + documentation) — **ID: TBD (community initiative; not a repo issue yet)**
- **Current Status:** Proposed in Discord by carlgatz; seeking 5 developers to stress test; no formal issue/PR in provided data.
- **Impact Assessment**
  - **User Impact:** **Medium** (could significantly reduce token cost/latency for high-volume workloads)
  - **Functional Impact:** **No** (optimization; not required for core operation)
  - **Brand Impact:** **Medium** (performance leadership is a differentiator if validated)
- **Technical Classification**
  - **Issue Category:** Performance / Feature Exploration
  - **Component Affected:** Potentially Model Integration / Inference pipeline / MCP usage patterns
  - **Complexity:** **Complex solution** (architecture + benchmarking + integration decisions)
- **Resource Requirements**
  - **Required Expertise:** gRPC, high-throughput data pipelines, benchmarking, cost modeling, agent tool routing
  - **Dependencies / Blockers:** Needs a formal design doc + benchmark harness; integration boundaries with elizaOS runtime must be defined
  - **Estimated Effort (1–5):** **4**
- **Recommended Priority:** **P3** (promote to P2 if it becomes a near-term product requirement)
- **Specific Actionable Next Steps**
  1. Open a GitHub discussion/issue to capture the architecture and stress test plan.
  2. Define benchmark scenarios (throughput spikes, token-cost reduction, tail latency).
  3. Identify integration points (is this a plugin? a reference deployment? an optional “edge preprocessor”?).
- **Potential Assignees**
  - **Primary:** carlgatz (author), rsn6958 / neural.x (new community devs with RAG/agent infra background)
  - **Supporting:** NubsCarson (orchestrator + cloud scale context)

---

# Highest Priority Summary (Top 5–10 to address immediately)
1. **#8257 — Steward login broken in prod** (P0): restore “Sign in with Eliza Cloud” end-to-end (OAuth + email).
2. **#8321 — Cloud Apps go-live blockers** (P0): ingress installed + verified routing, DB pooling/connection ceilings, env-var isolation contract verification.
3. **Android build synchronization (issue TBD)** (P1): unblock web-less APK/Capacitor sync; add CI guardrails.
4. **#8321 billing idempotency / double-bill risk (item #10)** (P0): unique constraints + resumable cron; remove risky side effects.
5. **#8294 — plugin-signer registry entry review** (P2): quick merge to keep ecosystem contributions flowing.
6. **gRPC token-optimization stress test initiative (TBD)** (P3): formalize into an issue + benchmark harness if it aligns with roadmap.

---

# Cross-Issue Patterns / Themes (possible deeper architectural signals)
- **Auth contract drift across services (Cloud ↔ Steward):** multiple points where frontend, worker, and upstream service expectations diverge (PKCE enforcement, signing/HMAC guards, email endpoint behavior). This suggests the need for tighter versioning and integration tests at service boundaries.
- **Cloud Apps “alpha platform” gaps cluster around classic multi-tenant scaling hazards:** ingress, DB connection pooling, placement, network exhaustion, lifecycle cleanup, and billing idempotency. These are systemic platform concerns rather than isolated bugs.
- **Build/distribution fragility on mobile:** Android/Capacitor sync issues indicate the toolchain and generated artifacts need a single source of truth plus CI enforcement.

---

# Process Improvement Recommendations
1. **Add synthetic canary tests for critical user journeys** (at minimum: Cloud login OAuth + email magic link) running against staging and production on a schedule.
2. **Introduce “go-live checklist” gates for Cloud Apps** (ingress reachable, tenant DB actually used, pooling present, multi-node routing validated, billing idempotency enforced) before enabling production flags.
3. **Require tracking issues for “blocked” work mentioned in dev logs** (e.g., Android build sync) to avoid invisible blockers and ensure ownership, repro steps, and acceptance criteria.
4. **Contract tests for cross-service integrations** (Cloud Worker ↔ Steward): versioned API expectations + signed request requirements captured as tests, not tribal knowledge.
5. **CI guardrails for generated/config drift** (Capacitor plugin manifests, route generation, auth redirect URIs): fail fast with clear remediation steps.