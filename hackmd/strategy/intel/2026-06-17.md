# ElizaOS Intel — 2026-06-17

## 1) Data Pattern Recognition (Velocity, Engagement, Adoption, Pain-Point Correlation)

### Dev velocity & change profile (rolling month context)
- **Repo activity (2026-06-01 → 2026-07-01 to date):** **302 PRs opened / 217 merged**, **24 new issues**, **30 issues closed**, **28 active contributors**.
- **Work mix signal (from major contributors’ summaries):**
  - Heavy emphasis on **cloud reliability + auth hardening + infra automation** (Steward PKCE/HMAC signing, CORS/connectivity, provisioning, Terraform workflows).
  - Continued push on **orchestrator/workbench UI parity** and modularization (LifeOps decomposition into focused plugins).
- **Critical open scalability thread:** Issue **#8321** (“Apps data-plane scaling + ingress gaps before go-live”) remains a high-signal risk cluster (ingress, DB pooling, multi-node routing, autoscale, billing idempotency). Several follow-up PRs referenced in the month feed suggest progress, but **#8321 is still open** → treat as “not yet fully de-risked”.

### Community engagement patterns (Discord last 72h)
- **06-16:** Minimal technical discussion; mostly greetings and links. Two “product/hiring” queries; answers were essentially “no hiring” and “robot idea fell apart”.
- **06-15:** Higher signal day:
  - Confirmed **repo consolidation** into `eliza` (organizational win).
  - **Scam attempt** (ai16z token migration) detected quickly; community self-moderation worked.
  - **Collaboration proposal** surfaced: ERC-7710 Delegation Framework + Intuition Knowledge Graph, with **5 missions / $7,500 USDC**.
  - **Automod friction** blocking proposal posting → pushed to DM.
- **06-14:** Support-heavy pain points with **no resolutions provided** (blank UI on Windows; elizaCloud invitation/tenant access failures).

**Engagement pattern summary:** technical demand is present (support + platform access), but channel throughput to resolution is low. The community can detect scams quickly, but struggles to close product/engineering troubleshooting loops.

### Feature adoption / usage signals (weak but notable)
- “Cron Job” recap referenced:
  - **Coding workbench parity** milestone (Claude Code/Codex parity claim).
  - **Cloud login outage** occurred recently (and was addressed previously).
  - **plugin-lifeops decomposition** (adoption risk: breaking changes + onboarding).
  - “Surge of DeFAI builders” (demand signal for onboarding + stability).

### Pain point correlation across channels
Recurring clusters across Discord + repo themes:
1. **Cloud access/auth & onboarding** (tenant invitation requirement errors; prior login outages; multiple auth/CORS fixes in repo).
2. **Local developer experience on Windows** (blank/white UI screen with `bun run dev:ui`; Windows env/path pitfalls show up elsewhere in codebase historically).
3. **Information freshness & visibility** (Milady “silent GitHub” concern; repo consolidation not broadly “announced as canonical”; dead links for product inquiries).
4. **Moderation + routing** (off-topic posts in coders; automod blocking legitimate proposal submission).

---

## 2) User Experience Intelligence (Feedback Themes, Impact, Sentiment, Opportunities)

### Feedback themes by impact
**High impact (blocking)**
- **Windows dev UI blank/white screen** when running `bun run dev:ui` (agent runs; skills sync; browser blank across Chrome/Firefox; cache clear didn’t help).  
  - Impact: blocks new devs on Windows from contributing/testing UI.
  - Status: unresolved in-channel (no workaround shared).
- **elizaCloud access blocked by “tenant invitation required”** (users cannot join; cannot access via Milady; can’t modify OpenRouter API settings).  
  - Impact: blocks product onboarding and paid usage; likely drives churn/support load.
  - Status: unresolved in-channel.

**Medium impact (trust/clarity)**
- **Hiring ambiguity** → repeated questions; answer “not hiring” but no public canonical pointer.
- **Dead external links** (humanoid robot ordering; news link dead) → credibility/expectation management issue.
- **Milady development silence** perception → user uncertainty about roadmap/maintenance.

**Low impact (channel health)**
- Off-topic crypto solicitations in coders channel dilute support capacity.

### Usage patterns vs intended design
- Users treat Discord as **first-line support + product access help desk**, but the system behaves like a **social channel** with limited closure (few “solved” threads, few pinned workarounds).
- Proposal submission is intended to be open/community-facing, but **automod forces private DM**, reducing transparency and reuse.

### Sentiment (qualitative)
- Sentiment is mixed: neutral-to-positive on milestones (repo consolidation, workbench parity), but **frustration/risk** around access blockers (tenant invites) and dev friction (Windows blank screen). Scam detection event shows community vigilance.

### Implementation opportunities (near-term UX wins)
1. Publish a **single “Cloud Access / Invites / Tenants” canonical doc** + link it in Discord + error UI.
2. Create a **Windows-specific “dev:ui blank screen” troubleshooting guide** with minimum repro + log capture steps.
3. Add a **Discord support intake template** (OS, command, logs, screenshot) and a lightweight “solved” tagging flow.

---

## 3) Strategic Prioritization (Impact vs Risk, Dependencies, Resourcing)

### Priority stack (next 7–14 days)

#### P0 — Unblock onboarding & revenue paths
1) **Resolve elizaCloud tenant invitation requirement failures**
- **User impact:** Very high (blocks product access).
- **Technical risk:** Medium (likely auth/tenant config + UX messaging).
- **Dependencies:** Steward/tenant configuration, frontend error handling, invitation flows.
- **Recommendation:** assign a single DRI across cloud-frontend + cloud-api; ship:
  - clear UI state (“You need an invite” + CTA/action),
  - admin/self-serve invite mechanism or request flow,
  - status page note + workaround if applicable.

2) **Fix Windows `bun run dev:ui` blank screen**
- **User impact:** High for contributor growth.
- **Technical risk:** Medium (bundler/runtime mismatch, environment differences, routing, or plugin load failures).
- **Recommendation:** treat as a tracked issue with a deterministic repro checklist; add CI smoke for Windows UI boot if missing.

#### P1 — Close the “Apps go-live” critical path risks
3) **Drive #8321 to closure (Apps data-plane scalability + ingress)**
- **User impact:** High (Apps hosting is a flagship capability).
- **Technical risk:** High (ingress, DB pooling, multi-node routing, billing/idempotency, egress controls).
- **Dependencies:** infra (Terraform), runtime (container placement), database layer (pooling), billing cron hardening.
- **Recommendation:** convert #8321 into an explicit launch checklist with acceptance tests (reachability, isolation active, connection ceilings, multi-node ingress).

#### P2 — Community → execution conversion
4) **ERC-7710 Semantic Delegation collaboration proposal**
- **User impact:** Potentially high (delegation + knowledge graph aligns with agent autonomy direction).
- **Technical risk:** Medium-to-high (protocol integration + product fit).
- **Recommendation:** timebox discovery: 1-hour technical scoping call + written integration plan; pick **one** mission as a pilot (tutorial or POC) before committing to production “Liquid Democracy”.

### Resource allocation recommendation
- **60%**: Cloud onboarding/access + Apps data-plane readiness (P0/P1).
- **25%**: DevEx reliability (Windows/local dev, docs, fast repro).
- **15%**: Ecosystem growth experiments (ERC-7710 pilot, moderation/process improvements).

---

## Quantitative KPIs to Track (starting today)
- **Support closure rate (Discord):** % of help requests with a documented workaround/solution within 48h (baseline last 3 days ≈ low; target ≥50%).
- **Onboarding funnel health:** tenant-invite error occurrences/day; successful first login rate; time-to-first-agent.
- **Windows DevEx:** % of Windows users who can run `dev:ui` successfully (survey + issue reports).
- **Apps readiness gates:** max concurrent apps per node before DB connection exhaustion; ingress reachability SLA; multi-node routing correctness.

---

## Actionable Recommendations (Immediate Next Steps)
1. **Open/confirm two tracking issues (if not already):**
   - “Windows `bun run dev:ui` blank screen” with required logs + minimal repro repo state.
   - “elizaCloud tenant invitation required blocks onboarding” with repro steps + expected behavior.
2. **Ship docs + pinned Discord posts within 24–48h:**
   - “Cloud Access & Invitations” doc (include tenant concepts, where to request invite, common errors).
   - “Windows Dev UI Troubleshooting” doc (Bun version, node/bun env, console errors, `--debug`, clearing state dirs).
3. **Operationalize proposal intake:**
   - Replace DM-only with a **#proposals-intake form** or bot command that bypasses automod but preserves transparency (auto-post summary + link to full proposal).
4. **Moderation hygiene:**
   - Add a lightweight rule + redirect bot for off-topic solicitations in coders; preserve signal for technical help.

---

## Risks & Watchouts
- **Mismatch between perceived progress and user reality:** recent “parity” milestones won’t matter if cloud access and Windows dev onboarding remain broken.
- **Apps data-plane launch risk:** if #8321-class issues remain partially unresolved, a premature go-live could create high-severity incidents (unreachable apps, DB exhaustion, mis-billing).
- **Trust surface:** dead links + ambiguous product/hiring messaging compound credibility drift; fix via canonical status/FAQ pages.