---
title: Create a plugin with the CLI
description: "Scaffold a new Eliza plugin package."
---

```bash
npx elizaos create [name] --template plugin
```

## Flags

| Flag                              | Purpose                                                  |
| --------------------------------- | -------------------------------------------------------- |
| `-t, --template plugin`           | Use the plugin template.                                 |
| `-l, --language <lang>`           | TypeScript (default).                                    |
| `-y, --yes`                       | Skip confirmation prompts.                               |
| `--description <description>`     | Plugin description (written into package.json).          |
| `--github-username <username>`    | GitHub owner (written into package.json repository).     |
| `--repo-url <url>`                | Override the generated repository URL.                   |

## What you get

The `plugin` template scaffolds an elizaOS plugin starter:

- `src/index.ts` — the package entry point.
- `src/plugin.ts` — starter action, provider, service, route, event, model, and test registrations.
- `src/__tests__/` and `src/e2e/` — component and end-to-end harnesses.
- `src/frontend/` — optional plugin UI.
- `build.ts` — Bun build plus declaration output into `dist/`.
- `package.json` — scoped under your namespace, ready for `npm publish`.

## Next steps

```bash
cd <name>
bun install
bun run build
bun run test
```

To iterate against a real agent, link the plugin into a project:

```bash
cd ../my-agent
bun add link:../my-plugin
```

Add `'my-plugin'` to your character's `plugins` array.

## Contribution scope

elizaOS no longer accepts third-party plugins or registry items. Contributions
focus on maintained first-party capabilities. See the
[contribution policy](https://github.com/elizaOS/eliza/blob/develop/CONTRIBUTING.md).

## See also

- [Plugin track](/tracks/plugin/overview)
- [Plugin anatomy](/tracks/plugin/anatomy)
- [Publish](/tracks/plugin/publish)
