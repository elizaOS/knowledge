# Launch QA

Launch QA collects the static release gates used before promoting desktop,
mobile, cloud, and model-data changes. The root `test:launch-qa` script runs
the fast local checks for documentation links, mobile artifact manifests, model
data fixtures, and focused package tests.

